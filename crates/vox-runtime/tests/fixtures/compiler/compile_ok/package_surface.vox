package fixtures.syntax.package;

/// Re-exported package import.
public import fixtures.host;
private import fixtures.tools;

/// Exported immutable value.
public val exported: Int = 1;
private var internal: String = "internal";

fun defaulted(x: Int = 1, y: String = "a",): String = y;

public fun pair[A: Any, B: Any](a: A, b: B): (A, B) = (a, b);

private evil fun sideEffect(): Unit {
    panic "package panic";
}
