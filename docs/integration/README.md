# Language Integration

This section covers host-language integration points for Vox.

Use it when you need to embed the runtime, expose host packages, or understand
what the runtime expects from a language-specific frontside.
