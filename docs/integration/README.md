# Language Integration

This section covers host-language integration points for Vox.

Use it when you need to embed the runtime, export host libraries, or understand
the host metadata that Vox consumes.
